# frozen_string_literal: true

require_relative "../spec_helper"

RSpec.describe "compute integration" do
  it "is pending until Dawn runtime is available" do
    skip "requires Dawn shared library and GPU runtime"
  end
end
